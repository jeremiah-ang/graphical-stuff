function Point (x, y, z = 0, pitch = 0, roll = 0, yaw = 0) {
	this.x = x;
	this.y = y;
	this.z = z;

	// pitch up to down 
	// roll side ways 
	// yaw left to right

	this.pitch = pitch;
	this.roll = roll;
	this.yaw = yaw;
}